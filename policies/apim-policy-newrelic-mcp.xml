<!--
  New Relic MCP — API-level policy.

  Rendered by Terraform templatefile(): $${...} placeholders are TF vars,
  {{...}} are APIM named values.

  Flow: validate Entra JWT (dual audience, v1+v2 issuers) -> audit -> per-user rate
  limit -> strip client auth + inject New Relic key. Routing to New Relic's hosted
  MCP is native to the type=mcp API (backendId + mcpProperties), so the policy does
  not route. New Relic uses a static Api-Key (no OAuth token exchange), so this is
  much simpler than the Salesforce reference.

  AuthZ is enforced at token ISSUANCE, not here: the enterprise app has
  appRoleAssignmentRequired=true, so Entra only mints a token for direct members of
  an app-assigned access group (the NR MCP access group + the assigned job-role
  groups). The gateway therefore trusts any valid, audience-scoped token from our
  tenant and does NOT re-check a `groups` claim — v1 tokens from
  `az account get-access-token` don't emit one, and nested-group membership never
  flows into the ApplicationGroup claim anyway.

  NO response buffering: MCP is streamable HTTP (chunked / text/event-stream),
  so the policy never reads the response body.
-->
<policies>
  <inbound>
    <base />

    <!-- 1. Client authN: validate the Entra JWT. AuthZ (who is allowed) is enforced
            upstream at token issuance via appRoleAssignmentRequired=true on the
            enterprise app, so this only proves the token is a genuine, unexpired
            Entra token minted for THIS app (audience) by OUR tenant (issuer).
            - openid-config points at the v2 metadata (its JWKS validates v1 + v2
              signatures — same signing keys).
            - <issuers> accepts BOTH the v1 (sts.windows.net) and v2
              (login.microsoftonline.com/.../v2.0) issuers, so a v1 token from
              `az account get-access-token` and a v2 token from an MSAL client are
              both accepted.
            - Dual audience accepts both the api:// URI and bare-GUID forms. -->
    <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized. A valid Entra token for the New Relic MCP gateway is required (membership in an assigned access group).">
      <openid-config url="https://login.microsoftonline.com/${tenant_id}/v2.0/.well-known/openid-configuration" />
      <audiences>
        <audience>api://{{newrelic-mcp-app-id}}</audience>
        <audience>{{newrelic-mcp-app-id}}</audience>
      </audiences>
      <issuers>
        <issuer>https://sts.windows.net/${tenant_id}/</issuer>
        <issuer>https://login.microsoftonline.com/${tenant_id}/v2.0</issuer>
      </issuers>
    </validate-jwt>

    <!-- 2. Audit: extract caller identity + correlation id. -->
    <set-variable name="userOid" value="@{
      var authHeader = context.Request.Headers.GetValueOrDefault(&quot;Authorization&quot;, string.Empty);
      if (!string.IsNullOrEmpty(authHeader) &amp;&amp; authHeader.StartsWith(&quot;Bearer &quot;)) {
        try {
          var jwt = authHeader.Substring(7).AsJwt();
          if (jwt != null) {
            var oid = jwt.Claims.GetValueOrDefault(&quot;oid&quot;, string.Empty);
            var upn = jwt.Claims.GetValueOrDefault(&quot;upn&quot;, string.Empty);
            return !string.IsNullOrEmpty(oid) ? oid : upn;
          }
        } catch { }
      }
      return &quot;anonymous&quot;;
    }" />
    <set-header name="x-apim-user-id" exists-action="override">
      <value>@((string)context.Variables["userOid"])</value>
    </set-header>
    <set-variable name="correlationId" value='@(context.Request.Headers.GetValueOrDefault("x-correlation-id", Guid.NewGuid().ToString()))' />
    <set-header name="x-correlation-id" exists-action="override">
      <value>@((string)context.Variables["correlationId"])</value>
    </set-header>

    <!-- 3. Per-user flood/cost guardrail. counter-key = user OID so one noisy
            caller cannot starve the shared New Relic ingest budget (DECISION #3). -->
    <rate-limit-by-key
      calls="${rate_limit_calls}"
      renewal-period="${rate_limit_period_seconds}"
      counter-key="@((string)context.Variables[&quot;userOid&quot;])"
      remaining-calls-header-name="x-ratelimit-remaining"
      retry-after-header-name="Retry-After" />

    <!-- 4. Backend authN: strip the client's Entra JWT, inject the New Relic
            User key from the secret named value. The client never holds it.
            Routing to New Relic is handled natively by the type=mcp API
            (backendId + mcpProperties) — no set-backend-service here. -->
    <set-header name="Authorization" exists-action="delete" />
    <set-header name="Api-Key" exists-action="override">
      <value>{{nv-newrelic-mcp-api-key}}</value>
    </set-header>
    <set-header name="Ocp-Apim-Subscription-Key" exists-action="delete" />
  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <!-- No body read — MCP streaming passes through untransformed. -->
    <base />
    <set-header name="x-correlation-id" exists-action="override">
      <value>@((string)context.Variables["correlationId"])</value>
    </set-header>
    <set-header name="x-apim-mcp-user" exists-action="override">
      <value>@((string)context.Variables["userOid"])</value>
    </set-header>
  </outbound>

  <on-error>
    <base />
    <set-header name="x-correlation-id" exists-action="override">
      <value>@(context.Variables.ContainsKey("correlationId") ? (string)context.Variables["correlationId"] : Guid.NewGuid().ToString())</value>
    </set-header>
  </on-error>
</policies>
